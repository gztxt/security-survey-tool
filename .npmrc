# Windows + UNC 工作区：cmd.exe 不支持 UNC 作为当前目录，必须让 pnpm 用 bash 执行
# package.json scripts，否则子进程（tsc/vite 等）启动失败。Linux/macOS 上此项无影响。
#
# 这里只写裸名 `bash`（走 PATH 解析，Git Bash 安装后通常在 PATH 中）。
# 如果你的 bash 不在 PATH 上，请把绝对路径写进**用户级** ~/.npmrc，不要提交到仓库：
#     script-shell=<你的绝对路径>/bin/bash.exe
script-shell=bash
