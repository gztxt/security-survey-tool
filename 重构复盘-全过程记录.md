# 安防勘点工具收敛重构 · 全过程复盘与总结

> **文档性质**：开发全过程复盘 —— 记录本次收敛重构（T1–T5）从侦察、决策、实现到交付回流的完整思考过程与经验总结。
> **日期**：2026-09-11
> **代码基线**：`baseline-pre-refactor` → `HEAD`（共 11 个 commit）
> **配套文档**：`README.md`（能力边界与命令）、`docs/回流交付说明.md`（回流与验证）、`方案/PRD-安防勘点工具收敛.md`（需求）、`方案/架构设计-安防勘点工具收敛.md`（技术方案与决策全文）

---

## 一、背景与目标

安防勘点工具是一个 **Electron 28 + Vue 3 + Pinia + Element Plus + TypeScript** 的 pnpm monorepo 桌面应用（约 29k 行 TS），核心用途：导入 CAD 图纸/图片/PDF 作为底图 → 在其上标注摄像头点位、网络走线、机柜位置 → 导出为图片/PDF/DXF 图纸。

侦察发现它的问题集中在 **"骨架空转 + 界面臃肿"**：

- 主进程 33+ 个 IPC handler 与 exporter 后端方法**真实存在且完整**，但**前端大量未接线**——`Toolbar.vue` 的导入/校准是空函数，保存只 `markClean()` 不落盘。
- 路由表大量重复别名、源码树混有 23 个 `.bak-*` 与 `__pycache__`、无 git 仓、无 README。
- 页面臃肿：DeviceLibraryView 1364 行、HelpView 1148 行、LayerManagerPanel 944 行，对主流程是噪音。

**用户诉求（原话要点）**：只需核心流程「导入 CAD/图片/PDF → 标注 → 导出图片/PDF/CAD」；其他功能收进「高级功能」菜单，**不删代码但默认不可见**；界面简化、聚焦主流程；不能丢数据、注意路径引用。

**本次铁律**：**不删任何功能代码，只改可见性与入口**。所有收敛均为"归档/隐藏/降级入口"，为后续迭代保留完整能力。

---

## 二、侦察阶段：三项决定性发现

在动笔前对存量代码做了实机核对，发现三条 **PRD 未记载、但直接影响工作量** 的硬事实：

| # | 发现 | 证据与影响 |
|---|---|---|
| **D-1** | **`window.api` 与 `window.electronAPI` 是两套名字，桥接根本没通** | preload 只暴露了 `electronAPI`，但 3 个 store 全用 `window.api.*` 调用，运行时 `window.api === undefined`。**这才是"空接线"的根因之一**：即便 `importDrawing()` 去调 store，也会在 `window.api.fs.showOpenDialog` 处抛 TypeError。必须先修桥接，否则一切接线都是纸面接线。 |
| **D-2** | **`Toolbar.vue` 根本没被挂载到任何路由** | 它只被 `CanvasView.vue` 引用，而路由表没有任何一条指向 CanvasView（图纸编辑挂的是 `DrawingView.vue`，里面另有同名的空 `importDrawing()`）。"三处空接线"实际要修的是**两处宿主**。 |
| **D-3** | **图片/PDF 位图底图链路完全不存在** | `importDrawings()` 对 png/jpg/pdf 只建记录不加载内容；渲染侧 `drawImage()` 是空注释；`fs:readFile` 固定按 UTF-8 读文本无法读二进制。拖入 PNG 会画布全黑。这是**新建能力**，不是"接线"。 |

另核实到两处"虚头支票"：
- `ExportFormat` 类型声明里含 `'dwg'/'docx'`，但 `Exporter.export()` 六条 filter 只放行 png/jpg/pdf/svg/xlsx/csv，**DXF/DWG 导出能力实际不存在**（全库 grep `dxf` 零命中）。用户诉求中的"导出 CAD"是**能力缺口**，不是"藏起来的功能"。
- `.survey` 扩展名不一致（主进程保存框 `.survey`，store 默认名 `.ssproj`），属兼容隐患。

---

## 三、八项架构决策（思考过程核心）

> 完整"结论 / 理由 / 备选"见《架构设计》§2，此处记录每次决策的**核心判断与关键理由**。

1. **可见性开关机制**：feature flag 落在 `settings.ts` 新增单个布尔 `advancedMode`（默认 `false`），走 **localStorage** 通道持久化。理由：`settings:get/set` IPC 是**空桩**（getSettings 返回 `{}`），不能用它持久化；路由守卫会破坏别名跳转；单个布尔位 + `v-if` 渲染过滤，改动面最小、可整体回退。消费端渲染期过滤，不用守卫。

2. **DXF writer 选型**：**手写 DXF R12 ASCII，零新依赖**。理由：需求实体集极小且确定（LINE/CIRCLE/INSERT/TEXT/POLYLINE）；R12 ASCII 是全 CAD 软件兼容性最好的下限；现有 `dxf-parser` 恰好是读侧，可直接做"写出→读回"往返单测；npm 上的 `dxf-writer` 包年久失修且违背"不引新依赖"精神。

3. **导入接线（调用链 + 拖拽授权缺口）**：关键发现——主进程 `assertReadableCadPath` 只放行"userData 白名单"或"曾被对话框授权"的路径，而**拖拽路径未经对话框授权**，一旦走到 `parseCadFile` 必被拒。解法：新增 `fs:grantPaths` IPC 把拖拽路径注入授权白名单，打通 AC-1.1 拖拽导入。

4. **保存接线（saveProject 空转）**：把 store 的 `saveProject/saveProjectAs` 改为**委托主进程 `project:save`**（主进程实现本就完整），删掉 renderer 侧自行拼路径 + `fs.writeFile`（那条路在 D-1 下必抛且被白名单拒绝）。关键回归点：**保存失败/取消必须保留 `isDirty=true`**，不能误清脏标记（原 `markClean()` 是数据安全隐患）。`project:save` 的 handler 签名扩为可选第二参 `filePath?`（向后兼容扩展）。

5. **UI 收敛布局**：默认主界面精简态只露 6 类动作（导入/校准/布点/布线/撤销重做/导出），其余全部收进顶栏右侧「高级功能 ▾」下拉；设置收敛为「通用/高级/关于」三分区。**严格只做渲染条件（`v-if`/过滤数组），不注释路由、不删组件、不改业务逻辑**。

6. **工程卫生**：git 初始化 + 打 tag 基线（必须先做，后续所有 diff 以此为基线）；`.bak-*`/`__pycache__` 用 `git mv` 归档到 `_archive/`（内容零丢失）；补 README/`.gitignore`/验证脚本。

7. **验证链（本地 Windows，无 NAS 依赖）**：`install → packages build → vue-tsc --noEmit → vitest → build:app`。关键：workspace 包靠 `dist/*.d.ts` 供类型，改包后必须先 rebuild；desktop 验证只用 `build:app`（`build` 带 electron-builder，无 Electron 二进制必失败）。

8. **交付回流 NAS**：只回传 `git diff --name-only baseline HEAD` 的源码改动文件，**绝不包含** node_modules/releases/resources/oda/用户 `.survey`；`.bak-*` 的删除以"git mv 至 _archive"表达，NAS 端 tar 覆盖不删旧位置，需执行 `apply-archive-list.sh` 补归档。

---

## 四、T1–T5 任务执行全记录

### T1 · 工程基线与卫生（低复杂度，必须先做）

**目标**：建立 git 基线与可验证依赖链，为后续 4 个任务提供 diff 基线。

**思考**：顺序不可颠倒——必须先 `git init` + 首次 commit + `tag baseline-pre-refactor`，再动任何功能代码；`.bak-*` 归档必须用 `git mv`（保留历史与内容），配合 `_archive/.../MANIFEST.sha256` 清单做双向对齐，供 NAS 端 `apply-archive-list.sh` 幂等执行。

**落地**：`.gitignore`、`README.md`、`scripts/apply-archive-list.sh`、`scripts/verify.ps1`、`packages/exporter/src/fixtures.ts`（单测构造器，先建空壳供 T3 填充，避免 T3/T5 争抢同一文件）；41 个 `.bak-*` + 4 个 `__pycache__/*.pyc` 归档。

### T2 · 接线修复：桥接层 + 导入/校准/保存三闭环（高复杂度）

**目标**：PRD P0-1/P0-2/P0-3 的主承载——打通 D-1 桥接根因 + 三个空接线 + 2 个现网 bug。

**思考**：桥接名不匹配的修复选了"**在 preload 里同时暴露 `api` 与 `electronAPI` 两个 key**"（改动 1 行），而非全量改调用点——两条历史路径都可用、风险最低、可整体回退。校准/导入/保存的编排逻辑放 composable（`useBaselineImport` / `useCalibration`），**不塞进 store**，避免 DOM/pdfjs 污染单测。

**踩坑与修复**：
- 现网 bug 1：`ProjectTreePanel.vue` 的 `router.push({name:'import'})` 指向不存在的路由，点"导入"必抛 `No match for {"name":"import"}`。
- 现网 bug 2：`project:save` 只接受 1 个参数，需扩为 `(_, data, filePath?)` 可选参，才能支持"已知道路径则静默写盘、否则弹框"。
- 自动保存 `startAutoSave` 的回调是空的，补成 `await saveProject()`；但需加 `silent` 形参——无路径时静默返回，否则每 30s 弹一次保存框。
- 关闭前脏数据提示"保留"是 PRD 高估，实际全库无 `beforeunload` 实现，需新建。

**验证**：新增 `project.test.ts` 契约测试（mock electronAPI，断言成功清脏/失败保脏/无路径 silent 不弹框）。

### T3 · DXF overlay 导出能力（高复杂度，全新算法）

**目标**：填补 PRD §0-B 的能力缺口——手写 DXF R12 ASCII writer + IPC + UI + 往返单测。

**思考**：导出走**主进程直调 Exporter**（DXF 是纯文本、不需要 canvas），**不走** `delegateExportToRenderer`（那条 120s 超时的往返通道），少一层异步握手、少一类时序 bug。图层统一 `SS-` 前缀（overlay 不与底图图层重名）；设备用 `SSB-<modelId>` 块 + `INSERT`。

**关键工程点**：
- **Y 轴镜像**：DXF 是 Y 向上、画布 Y 向下，overlay 实体必须 `flipY`（按 `bounds.maxY` 镜像），否则 CAD 里方向颠倒。
- **非 ASCII 文本**：中文 label 用 `\U+XXXX` 编码，header `$DWGCODEPAGE` 写 `ANSI_936`，否则乱码。
- **孤儿块**：`INSERT` 的块名必须在 BLOCKS 段有定义，否则 CAD 打开是空图（最常见的静默失败）。

**验证**：`dxf-writer.test.ts` 6 项断言（图层数=7 且名称正确、实体数相等、LINE 端点镜像误差 <1e-6、INSERT 零孤儿、中文往返相等、`0\nEOF\n` 结尾）+ 用 `dxf-parser` 读回产物。

### T4 · 可见性收敛（中复杂度，文件多但每处改动小）

**目标**：把 P1/P2 功能收进「高级功能」菜单，默认精简态。

**思考**：风险在"隐藏后不可达"——靠**保留全部路由 + 经典模式开关**兜底；新增 `useVisibility.ts` 作为 `showTool/showPanel/navItems` 的**单一事实源**。路由去重结论：13 个大写别名**全都有引用、一个都不能删**，只能"停菜单入口 + 保留 name"。

**细节坑**：`showRuler/showGrid/snapEnabled` 默认值调整为 `false/true/true`，但 `settings.load()` 会覆盖旧 localStorage 值——需在 `load()` 里做"仅当 key 缺失时才用新默认"的处理，否则老用户看不到变化。

### T5 · 位图底图能力 + 全链验证与交付（高复杂度，能力从零到一）

**目标**：D-3 缺口——图片/PDF 作为底图 + 导出含底图 + 体积治理 + 交付包。

**思考**：位图底图用**"合成实体"方案**——把位图包装成 `type:'IMAGE'` / `layer:'BASEMAP'` 的图元塞进 `drawing.entities`，不新增数据字段。`.survey` 向后兼容论证成立：旧版本读到 `type:'IMAGE'` 走"其他类型简化处理"分支静默跳过，不崩不破坏。

**体积治理（关键）**：base64 内嵌会让 `.survey` 暴涨（A0 图纸 2x 栅格化 ~15-40MB）。落地方案：会话内 dataURL 内联 → 保存时 `buildSavePayload` 剥离 dataURL、只留 `sourcePath/pageIndex` → 打开时按 sourcePath 重新栅格化（重水合）；原图缺失则提示"底图文件已移动，请重新链接"，标注数据不丢。

**三个技术深坑**：
1. **导出链路认知**：`export:pointMap` 实际走 `delegateExportToRenderer` → 渲染进程 DOM 重绘（**非快照路径**）。所以位图底图要入导出物，必须在 exporter 的 `drawEntityGeometry` 补 `case 'IMAGE'`，不能只靠快照。
2. **pdfjs 顶层 await**：`pdf.worker.min.mjs` 含顶层 await，默认 `target: es2020` 会报 "Top-level await is not available"，需 `vite.config.ts` 设 `build.target: 'es2022'`（Electron 28 = Chromium 120 支持）。
3. **重水合时序**：必须在 `loadProject` 里 `setProject` **之前** await 重水合，避免 CanvasViewport 二次 registerImage 时序问题。

---

## 五、验证结论与诚实声明

**已全绿（本地实测）**：
- `vue-tsc --noEmit`：exit 0
- `vitest`：desktop 15 绿 + exporter 16 绿（含 dxf-writer 往返测试）
- `vite build`：renderer + main + preload 三产物齐全，pdfjs worker 资源正常产出

**验证缺失项（如实声明，需 Electron 实机冒烟）**：以下 6 项依赖 Electron 图形环境，本地无法自动化验证，已写入 `docs/回流交付说明.md` §5.1：
1. 拖入 `.png/.jpg` → 位图底图与标注层同步缩放平移；
2. 多页 `.pdf` → 选页对话框（默认首页、scale=2）；
3. `BASEMAP` 图层不可误选、可整体隐藏；
4. 保存后体积不暴涨 + 重开重水合 + 原图改名提示；
5. 点位图导出含底图+点位+走线+比例尺+图例；
6. 底图为图片/PDF 时导出 DXF 的 AC-7.4 告知文案。

---

## 六、NAS 交付回流的 CRLF 事故与修复

**事故**：首次 `git archive` 生成的 `changes.tar` 解压到 NAS 后，`main.ts` 字节数 35493→36482、sha256 失配。

**根因**：本机 `core.autocrlf=true` 且无 `.gitattributes`，`git archive` 把 LF blob 转成 CRLF 写进 tar，污染整个交付包（`apply-archive-list.sh` 也混入 59 个 CR，在 Linux NAS 上无法正常执行）。

**修复**：
1. 用 `git -c core.autocrlf=false archive` 重新生成 `changes.tar`（LF 纯净）。
2. 新增 `.gitattributes`（`*.sh` 锁 LF、`*.ps1/*.bat/*.cmd` 锁 CRLF）杜绝复发。

**NAS 端最终验证**：
- 57 文件 sha256 全量比对：**ok=57 / mismatch=0 / missing=0**
- `main.ts` = 35493B / `a5eabb3d`；`apply-archive-list.sh` = 1853B / `0c8f548d` / 0 CR
- 归档脚本：moved=46 + skipped=1，复跑 moved=0（幂等）
- 残留 `.bak-*` / `__pycache__` 归零；node_modules(651M)/releases(445M)/resources 未触碰
- 回滚安全网：`安防勘点工具.predeploy-20260911_230520/source.tar.gz`（287M）保留在位

---

## 七、关键工程认知（经验教训）

1. **渲染进程有两个桥接名**（`window.electronAPI` 与 `window.api`）——接任何线之前先确认 `window.api` 非 undefined，否则运行时必抛。
2. **不要相信 PRD 的乐观表述**——本次至少 4 处 PRD 高估：saveProject 不只是 markClean（store 层也坏）、"关闭前脏提示"实际不存在、"图片/PDF 底图"实际未实现、Toolbar 三处空接线实际有两处宿主。
3. **`settings:get/set` IPC 是空桩**——任何需要持久化的开关一律走 settings store 的 localStorage 通道。
4. **路径安全白名单**——renderer 递给主进程的文件路径若来自拖拽/外部，必须先 `fs:grantPaths` 或 `fs:showOpenDialog` 授权。
5. **导出链路两跳别混淆**——`export:pointMap` 走 `delegateExportToRenderer`（DOM 重绘），新增的 `export:dxf` 走主进程直调，别塞进 delegate 白丢时序风险。
6. **DXF 三件套**——flipY 镜像、`\U+XXXX` 中文编码、INSERT 块定义必在 BLOCKS 段，否则方向反/乱码/空图。
7. **CRLF 检测别用 `grep -c $'\r'`**——在 Git Bash 下 `$'\r'` 会被吞成空模式、误报全行数，正确用 `tr -cd '\r' | wc -c`。
8. **ssh 传多行命令**——每条 `printf` 独立一行时只有最后一条被管道给远程，多行脚本要用单个 `printf '%s\n' '行1' '行2' ... | ssh_run.py`。
9. **构建顺序**——workspace 包改后必须 rebuild（desktop 靠 `dist/*.d.ts` 做类型）；desktop 验证只用 `build:app`。

---

## 八、已知限制与后续建议

**已知限制**：
- 双份 Python sidecar（`apps/desktop/resources/python/` 与根 `resources/python/`）为历史遗留重复副本，本次**未动**，待后续单独收敛。
- DWG 直接回写明确不做（格式专有 + ODA 依赖），DXF 导出的底图边界：图片/PDF 底图时 DXF 只含矢量标注层。
- 6 项 Electron 实机冒烟项待现场验证（见 §五）。

**后续建议**：
1. 在 NAS 所在主机做一次"新手主流程冒烟"（导入 CAD/图片/PDF → 布点 → 导出），确认运行时无回归。
2. 双份 Python sidecar 去重（收敛为一份，打包 `extraResources` 与 dev 路径统一）。
3. 如老用户需全功能界面，在「设置 → 高级」打开「经典模式」即可一键回退（`advancedMode` 开关已内置）。

---

*本文档与代码基线 `HEAD` 同步。任何后续改动请在对应任务段追加记录，保持"思考过程 + 决策 + 踩坑 + 总结"四要素齐全。*
